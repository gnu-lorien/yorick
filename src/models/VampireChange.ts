import Parse from 'parse/dist/parse.min.js'
export class VampireChange extends Parse.Object {
  constructor(
    attributes?: Parse.Attributes,
    options?: any,
  ) {
    super('VampireChange', attributes, options)
  }
}
