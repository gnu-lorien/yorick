import Parse from 'parse/dist/parse.js'

export class ExperienceNotation extends Parse.Object {
  constructor(
    attributes?: Parse.Attributes,
    options?: any,
  ) {
    super('ExperienceNotation', attributes, options)
  }
}
