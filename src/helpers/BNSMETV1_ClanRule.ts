import Parse from 'parse/dist/parse.min.js'
export class ClanRule extends Parse.Object {
  constructor(attributes?: Parse.Attributes, options?: any) {
    super('ClanRule', attributes, options)
  }
}
