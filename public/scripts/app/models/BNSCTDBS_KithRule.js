// Category Model
// ==============

// Includes file dependencies
define([
	"parse"
], function( Parse ) {

    // The Model constructor
    var Model = Parse.Object.extend( "bnsctdbs_KithRule", {

    } );

    // Returns the Model class
    return Model;

} );